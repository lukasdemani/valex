import { NextFunction, Request, Response } from "express";
export async function cardMiddleware(req: Request, res: Response, next: NextFunction) {
    // const apiKey = req.headers["proxy-authorization"]
  
    // if (!token) {
    //   return res.sendStatus(401);
    // }
  
    // let user;
  
    // try {
    //   user = await verifyToken(token);
    // } catch {
    //   return res.sendStatus(401);
    // }
  
    // res.locals.user = user;
    // next();

    
  }